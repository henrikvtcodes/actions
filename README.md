# Henriks Actions
These are all my actions workflows. Set here for easy use.

## Workflows
This is all the available files/actions.
#### `react-nodejs.yaml`
This is a simple workflow for checking the validity of nodejs-compatible code. It runs `npm install` and then `npm run build`.
